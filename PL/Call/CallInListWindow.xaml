<Window x:Class="PL.Call.CallInListWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PL.Call"
        mc:Ignorable="d"
        Title="CallWindow" Height="640" Width="800"
        DataContext="{Binding RelativeSource={RelativeSource Mode=Self}}">


    <!--External Grid-->
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40*"/>
            <RowDefinition Height="500*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!--Internal, Upper Grid-->
        <Grid HorizontalAlignment="Stretch" Height="auto" Grid.Row="0" VerticalAlignment="Stretch" Width="auto">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <!-- Label -->
            <Label Content="Select Call Field" Grid.Column="0" VerticalAlignment="Center" HorizontalAlignment="Left"/>

            <!-- ComboBox -->
            <ComboBox Grid.Column="1" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" ItemsSource="{Binding Source={StaticResource CallInListCollectionKey}}"
                      SelectionChanged="CallComboBox_SelectionChanged"
                      SelectedValue="{Binding Path=CallSelectMenus, Mode=TwoWay}" />
        </Grid>
        <!--<DataGrid Margin="5" Grid.Row="1" ItemsSource="{Binding Path=CallList}"/>-->
        <DataGrid ItemsSource="{Binding Path=CallList}" 
                  IsReadOnly="True" 
                  AutoGenerateColumns="False"
                  EnableRowVirtualization="True">
                    <DataGrid.Columns>
                        <DataGridTextColumn Binding="{Binding Call.Id}" Header="Id" Width="Auto"/>
                        <DataGridTextColumn Binding="{Binding Call.CallId}" Header="Call Id" Width="Auto"/>
                        <DataGridTextColumn Binding="{Binding Call.CallType}" Header="Type" Width="Auto"/>
                        <DataGridTextColumn Binding="{Binding Call.OpenTime}" Header="Open Time" Width="Auto"/>
                        <DataGridTextColumn Binding="{Binding Call.TimeUntilAssigning}" Header="Time Until Assigning" Width="Auto"/>
                        <DataGridTextColumn Binding="{Binding Call.LastVolunteerName}" Header="Last Volunteer" Width="Auto"/>
                        <DataGridTextColumn Binding="{Binding Call.totalTreatmentTime}" Header="Total Treatment Time" Width="Auto"/>
                        <DataGridTextColumn Binding="{Binding Call.Status}" Header="Status" Width="Auto"/>
                        <DataGridTextColumn Binding="{Binding Call.NumberOfAssignments}" Header="Assignments" Width="Auto"/>


                        <!-- דוגמה לעמודה בוליאנית עם CheckBox (אם יש שדה כזה) -->

                        <!--<DataGridTemplateColumn Header="Is Active">
            <DataGridTemplateColumn.CellTemplate>
                <DataTemplate>
                    <CheckBox IsEnabled="False" IsChecked="{Binding IsActive}" />
                </DataTemplate>
            </DataGridTemplateColumn.CellTemplate>
        </DataGridTemplateColumn>-->

                    </DataGrid.Columns>
                </DataGrid>


                <Button Content="Add" Grid.Row="2" HorizontalAlignment="Center" Height="50" Width="100"/>
            </Grid>

        </Window>
