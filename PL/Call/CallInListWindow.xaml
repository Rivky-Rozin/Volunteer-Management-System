<Window x:Class="PL.Call.CallInListWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PL.Call"
        mc:Ignorable="d"
        Title="ניהול קריאות" Height="700" Width="1200"
        Loaded="Window_Loaded"
        Closed="Window_Closed"
        DataContext="{Binding RelativeSource={RelativeSource Mode=Self}}"
        Background="{DynamicResource WindowBackgroundBrush}"
         xmlns:controls="clr-namespace:PL.Controls">

    <Window.Resources>
        <SolidColorBrush x:Key="StatusOpenBrush" Color="#f8f9fa"/>
        <SolidColorBrush x:Key="StatusInProgressBrush" Color="#e0f7fa"/>
        <SolidColorBrush x:Key="StatusOpenAtRiskBrush" Color="#fff9c4"/>
        <SolidColorBrush x:Key="StatusInProgressAtRiskBrush" Color="#ffe0b2"/>
        <SolidColorBrush x:Key="StatusExpiredBrush" Color="#ffcdd2"/>
        <SolidColorBrush x:Key="StatusClosedBrush" Color="#c8e6c9"/>

        <Style x:Key="StatusBasedRowStyle" TargetType="DataGridRow">
            <Style.Triggers>
                <DataTrigger Binding="{Binding Status}" Value="Open">
                    <Setter Property="Background" Value="{StaticResource StatusOpenBrush}"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding Status}" Value="InProgress">
                    <Setter Property="Background" Value="{StaticResource StatusInProgressBrush}"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding Status}" Value="OpenAtRisk">
                    <Setter Property="Background" Value="{StaticResource StatusOpenAtRiskBrush}"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding Status}" Value="InProgressAtRisk">
                    <Setter Property="Background" Value="{StaticResource StatusInProgressAtRiskBrush}"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding Status}" Value="Expired">
                    <Setter Property="Background" Value="{StaticResource StatusExpiredBrush}"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding Status}" Value="Closed">
                    <Setter Property="Background" Value="{StaticResource StatusClosedBrush}"/>
                    <Setter Property="Foreground" Value="DimGray"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>
    
    <Grid Margin="24">
        
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
       
        <controls:ThemeToggleButton HorizontalAlignment="Right" VerticalAlignment="Top" 
                                    Margin="0,-14,-14,0" Panel.ZIndex="1"/>
        <!-- כותרת ראשית ואזור סינון -->
        <Border Grid.Row="0" Padding="18" Margin="0,0,0,24">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="200"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="רשימת קריאות"  FontSize="22" FontWeight="SemiBold" VerticalAlignment="Center" />
                <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                    <Label Content="סנן לפי שדה:" VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <ComboBox Width="140" ItemsSource="{Binding Source={StaticResource CallInListCollectionKey}}"
                              SelectionChanged="CallComboBox_SelectionChanged"
                              SelectedValue="{Binding Path=CallSelectMenus, Mode=TwoWay}"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- טבלת קריאות -->
        <Border Grid.Row="1" Padding="10" CornerRadius="0">
            <DataGrid Margin="0" ItemsSource="{Binding Path=CallList}" MouseDoubleClick="DataGrid_MouseDoubleClick" AutoGenerateColumns="False" CanUserAddRows="False"
                      RowHeight="38" HeadersVisibility="Column" GridLinesVisibility="All"
                      HorizontalGridLinesBrush="{StaticResource BorderBrush}" VerticalGridLinesBrush="{StaticResource BorderBrush}"  RowStyle="{StaticResource StatusBasedRowStyle}">
                <DataGrid.Columns>
                    <DataGridTextColumn Binding="{Binding Id}" Header="Id" Width="Auto"/>
                    <DataGridTextColumn Binding="{Binding CallId}" Header="Call Id" Width="Auto"/>
                    <DataGridTextColumn Binding="{Binding CallType}" Header="Type" Width="Auto"/>
                    <DataGridTextColumn Binding="{Binding OpenTime}" Header="Open Time" Width="Auto"/>
                    <DataGridTextColumn Binding="{Binding TimeUntilAssigning}" Header="Time Until Assigning" Width="Auto"/>
                    <DataGridTextColumn Binding="{Binding LastVolunteerName}" Header="Last Volunteer" Width="Auto"/>
                    <DataGridTextColumn Binding="{Binding totalTreatmentTime}" Header="Total Treatment Time" Width="Auto"/>
                    <DataGridTextColumn Binding="{Binding Status}" Header="Status" Width="Auto"/>
                    <DataGridTextColumn Binding="{Binding NumberOfAssignments}" Header="Number of Assignments" Width="Auto"/>
                    <DataGridTemplateColumn Header="Delete" Width="80">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button Content="Delete" Click="DeleteCall_Click" Tag="{Binding Id}" Height="28" Width="60">
                                    <Button.Style>
                                        <Style TargetType="Button">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                            <Style.Triggers>
                                                <MultiDataTrigger>
                                                    <MultiDataTrigger.Conditions>
                                                        <Condition Binding="{Binding Status}" Value="Open"/>
                                                        <Condition Binding="{Binding NumberOfAssignments}" Value="0"/>
                                                    </MultiDataTrigger.Conditions>
                                                    <Setter Property="Visibility" Value="Visible"/>
                                                </MultiDataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Button.Style>
                                </Button>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTemplateColumn Header="Cancel Assignment" Width="120">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button Content="Cancel Assignment" Click="CancelAssignment_Click" Tag="{Binding CallId}" Height="28" Width="100"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <!-- כפתור הוספה -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,24,0,0">
            <Button Content="הוסף קריאה חדשה" Height="44" Width="180" Click="AddNewCall_Click"      />
        </StackPanel>
    </Grid>
</Window>
