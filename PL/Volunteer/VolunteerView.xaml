<Window x:Class="PL.VolunteerView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PL"
        mc:Ignorable="d"
        Title="Volunteer Dashboard" Height="700" Width="800">

    <Window.Resources>
        <Style TargetType="Label">
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Margin" Value="0,5,0,0"/>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Margin" Value="0,0,0,5"/>
            <Setter Property="Padding" Value="3"/>
        </Style>
        <Style TargetType="Button">
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Margin" Value="5"/>
        </Style>
    </Window.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Main Title -->
        <TextBlock Grid.Row="0" Text="{Binding Name, StringFormat='Welcome, {0}!'}" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,20"/>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="20"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Volunteer Details Section -->
            <GroupBox Header="My Details" Grid.Column="0">
                <StackPanel Margin="10">
                    <Label Content="ID (Read-Only):"/>
                    <TextBox Text="{Binding Id}" IsEnabled="False"/>

                    <Label Content="Name:"/>
                    <TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}"/>

                    <Label Content="Address:"/>
                    <TextBox Text="{Binding Address, UpdateSourceTrigger=PropertyChanged}"/>

                    <Label Content="Phone Number:"/>
                    <TextBox Text="{Binding PhoneNumber, UpdateSourceTrigger=PropertyChanged}"/>

                    <CheckBox IsChecked="{Binding IsActive}" Content="I am currently active" Margin="0,10,0,10"/>
                    <Button Content="Update My Details" Command="{Binding UpdateVolunteerCommand}"/>
                </StackPanel>
            </GroupBox>

            <!-- Current Call Section -->
            <!-- *** תיקון: הוסרו הסוגריים המרובעים מסביב ל-Binding *** -->
            <GroupBox Header="My Current Call" Grid.Column="2" 
                      Visibility="{Binding CurrentCall, Converter={StaticResource NullToVisibilityConverterKey}}">
                <StackPanel Margin="10">
                    <Label Content="Call ID:"/>
                    <TextBox Text="{Binding CurrentCall.Id}" IsEnabled="False"/>

                    <Label Content="Description:"/>
                    <TextBox Text="{Binding CurrentCall.Description}" IsReadOnly="True" TextWrapping="Wrap"/>

                    <Label Content="Address:"/>
                    <TextBox Text="{Binding CurrentCall.Address}" IsReadOnly="True"/>

                    <Label Content="Status:"/>
                    <TextBox Text="{Binding CurrentCall.Status}" IsReadOnly="True"/>

                    <Label Content="Distance from me (km):"/>
                    <TextBox Text="{Binding DistanceToCall, StringFormat=F2}" IsReadOnly="True"/>

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,15,0,0">
                        <Button Content="Finish Treatment" Command="{Binding FinishCallCommand}" Background="LightGreen"/>
                        <Button Content="Cancel Treatment" Command="{Binding CancelCallAssignmentCommand}" Background="LightCoral"/>
                    </StackPanel>
                </StackPanel>
            </GroupBox>

            <!-- Message for when there is no call -->
            <TextBlock Grid.Column="2" Text="You do not have an active call." 
                       HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="16"
                       Visibility="{Binding CurrentCall, Converter={StaticResource InvertedNullToVisibilityConverterKey}}"/>
        </Grid>

        <!-- Map Section -->
        <Border Grid.Row="2" BorderBrush="Gray" BorderThickness="1" Margin="0,10"
                Visibility="{Binding CurrentCall, Converter={StaticResource NullToVisibilityConverterKey}}">
            <TextBlock Text="Map showing volunteer and call location" 
                       HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="Gray"/>
        </Border>

        <!-- Main Action Buttons -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Choose a Call to Handle" Command="{Binding ChooseCallCommand}"/>
            <Button Content="View My Call History" Command="{Binding ViewHistoryCommand}"/>
        </StackPanel>
    </Grid>
</Window>